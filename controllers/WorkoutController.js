const Workout = require('../models/Workout');

const getByUserID = async (user_id) => {
    let workout, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workout,
        msg,
        success,
    };
}

const createWorkout = async (workout_body) => {
    let workout, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workout,
        msg,
        success,
    };
}

const updateWorkout = async (user_id, update_fields) => {
    let workout, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workout,
        msg,
        success,
    };
}

const deleteWorkout = async (user_id) => {
    let workout, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workout,
        msg,
        success,
    };
}

module.exports = {
    getByUserID,
    createWorkout,
    updateWorkout,
    deleteWorkout
}
