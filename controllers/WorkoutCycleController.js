const WorkoutCycle = require('../models/WorkoutCycle');

const getByUserID = async (user_id) => {
    let workoutCycle, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workoutCycle,
        msg,
        success,
    };
}

const createWorkoutCycle = async (workout_cycle_body) => {
    let workoutCycle, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workoutCycle,
        msg,
        success,
    };
}

const updateWorkoutCycle = async (user_id, update_fields) => {
    let workoutCycle, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workoutCycle,
        msg,
        success,
    };
}

const deleteWorkoutCycle = async (user_id) => {
    let workoutCycle, msg;
    let success = false;

    try {

    } catch (err) {

    }

    return {
        workoutCycle,
        msg,
        success,
    };
}

module.exports = {
    getByUserID,
    createWorkoutCycle,
    updateWorkoutCycle,
    deleteWorkoutCycle
}
